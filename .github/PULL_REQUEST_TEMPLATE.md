## Summary

<!-- What and why. Link the issue if there is one. -->

## Test plan

- [ ] `./test/run.sh` passes
- [ ] `shellcheck -S warning -x sushi install.sh test/run.sh` is clean
- [ ] New behaviour has a test (or this is docs / templates only)
- [ ] macOS / bash 3.2 / BSD userland still considered — no `mapfile`, `declare -A`, GNU-only flags, bare `awk`

## Notes

<!-- Performance path? Config write path? Both history parsers? Say so. -->
